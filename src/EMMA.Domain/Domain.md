# Domain (EMMA)

The Domain layer holds the core media concepts and policy rules that the rest
of the runtime builds on. It is intentionally free of infrastructure concerns
and avoids IO, making it stable, deterministic, and easy to unit test.
