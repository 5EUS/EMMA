# Contracts (EMMA)

Contracts define the shared data shapes and request metadata that cross
process or boundary lines (core runtime, plugin host, API surface). This keeps
serialization stable and AOT-friendly while allowing the Application and
Infrastructure layers to evolve independently.
